# diagrams/

draw.io / Excalidraw sources live in `src/`; export PNG to the root of this directory
as `network-diagram.png` so the main README can embed it.

Recommended tool: [draw.io](https://draw.io) (free, offline-capable). Save the source
`.drawio` file in `src/` alongside the exported PNG so the diagram stays editable.
