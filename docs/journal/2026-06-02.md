# 2026-06-02

**Goal:** Audit the running homelab, reclaim storage for the security build, scaffold docs.

**Did:**
- Confirmed host state: Proxmox 9.1.1, 6 LXC services, single 512 GB SSD, flat network.
- Shrank all six LXC root disks (~54 GB allocated reclaimed). Confirmed thin pool ~7% used.
- Resolved an MMP lock on the Nextcloud (CT 101) resize via `tune2fs -E clear_mmp`.
- Defined the four-VLAN target architecture and the phase plan.

**Broke / fixed:** CT 101 failed to start mid-resize (MMP lock + interrupted shrink);
cleared MMP, re-ran e2fsck + resize2fs, container healthy.

**Learned:** Thin-pool "free" metadata ≠ actual free space; correct shrink ordering;
MMP protection behavior.

**Next:** Phase B — create `vmbr1` (VLAN-aware bridge) on Proxmox and build the pfSense VM.

---

**Update (Phase A close-out):**
- Took `vzdump` backups of all six LXC containers (CTIDs 100–105) to local storage.
- Confirmed backups present in Datacenter → Backup UI.
- Phase A complete. Cleared to begin Phase B.
