# 2026-06-03

**Goal:** Phase B continued — create VLANs 20/30/40 in pfSense, assign and configure
interfaces, enable DHCP on all new zones.

**Did:**
- Took Proxmox snapshot `post-wizard-clean` on VM 200 (pfSense) as rollback point.
- Created three VLAN sub-interfaces on `vtnet1` (LAN):
  - VLAN 20 — TRUSTED-MGMT
  - VLAN 30 — VULNERABLE
  - VLAN 40 — MONITORING
- Assigned all three as pfSense interfaces (OPT1 → OPT3); saved assignments.
- Configured each interface — enabled, renamed, set static IPv4:
  - OPT1 → TRUSTEDMGMT — `10.10.20.1/24`
  - OPT2 → VULNERABLE   — `10.10.30.1/24`
  - OPT3 → MONITORING   — `10.10.40.1/24`
- Enabled DHCP server on all three new interfaces, range `.100–.199` each.
- LAN (VLAN 10 SERVICES) DHCP was already configured (`10.10.10.100–199`).

**Current VLAN/DHCP state:**

| VLAN | Interface   | Gateway     | DHCP Range                |
|------|-------------|-------------|---------------------------|
| 10   | LAN         | 10.10.10.1  | 10.10.10.100–10.10.10.199 |
| 20   | TRUSTEDMGMT | 10.10.20.1  | 10.10.20.100–10.10.20.199 |
| 30   | VULNERABLE  | 10.10.30.1  | 10.10.30.100–10.10.30.199 |
| 40   | MONITORING  | 10.10.40.1  | 10.10.40.100–10.10.40.199 |

**Broke / fixed:** Nothing broken this session.

**Learned:** pfSense VLAN sub-interfaces ride on top of the physical LAN interface
(`vtnet1`), not WAN. Each VLAN needs to be created, then assigned, then configured as
a separate step — three distinct screens. The red "settings have already been applied"
banner after saving an interface is harmless.

**Next:** Write default-deny firewall rules between zones (Phase B step 3).
Then migrate the 6 LXCs into VLAN 10.
