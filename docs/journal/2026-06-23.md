# 2026-06-23

**Goal:** Finish Phase C — verify the Wazuh install on VM 201 and get agents reporting.

**Did:**
- Verified all three Wazuh services (manager/indexer/dashboard) active + enabled on VM 201.
- Recovered dashboard login: reset the `admin` password with `wazuh-passwords-tool.sh`
  (also rotated a credential that had been exposed in a screenshot).
- Added pfSense LAN rule: Pass `10.10.10.0/24 -> 10.10.40.10` TCP 1514-1515, placed above
  the `Block SERVICES -> MONITORING` rule so agent traffic isn't dropped by default-deny.
- Deployed Wazuh agents to Pi-hole (CT 100) and Nextcloud (CT 101); both active.

**Broke / fixed:**
- Agent install failed: `lsb-release` missing on the LXC template -> pre-installed it.
- Agent wouldn't start: config held literal `MANAGER_IP` (env var lost during the
  install-f repair) -> fixed with `sed`, agent connected.

**Learned:** default-deny means every new cross-zone flow (agent->manager) needs an
explicit allow *above* the block; first-match-wins ordering matters. Minimal LXC templates
need `lsb-release` for the agent. A one-pass install keeps `WAZUH_MANAGER` from being lost.

**Next:** Phase D — Suricata on pfSense (ETOpen rules), EVE JSON -> Wazuh.
